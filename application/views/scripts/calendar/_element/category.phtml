<ul class="secand_menu">
    <li>
        <a href="/<?php echo $this->linkInfo->link; ?>/">КАТЕГОРИИ</a>
        <ul>
            <?php if ($this->categoryList !== false): ?>
                <?php foreach ($this->categoryList as $item): ?>
                    <li>
                        <a href="<?php echo $this->url(array('controller' => 'calendar', 'action' => 'view', 'categoryId' => $item->id), $this->linkInfo->link . '-category') ?>"><?php echo $this->escape($item->title); ?></a>
                    </li>
                <?php endforeach; ?>
            <?php endif; ?>
        </ul>

    </li>
</ul>